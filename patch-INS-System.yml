---
- name: Patch Windows (Supports MSU and WSUS methods for IIS)
  hosts: all
  gather_facts: false
  vars:
    #target_kb_id: "KB5074975"
    #target_msu_path: "C:\\PatchTemp\\KB5074975.msu"
    #patch_method: 'msu' 
    #wsus_server_ip: "10.0.6.62"
    #wsus_computer_group: "All Computers"

  tasks:
    # ==========================================
    # Patch Method 1: MSU File (Default)
    # File is placed at C:\PatchTemp\KB5074975.msu
    # ==========================================
    - name: Executing Patch via stand-alone MSU file
      when: patch_method == 'msu'
      block:
        - name: Verify patch before installation (Pre-Install Check)
          ansible.windows.win_shell: |
            Try {
                $hotfix = Get-HotFix -Id {{ target_kb_id }} -ErrorAction Stop
                Write-Output "Installed"
            } Catch {
                Write-Output "Not Installed"
            }
          register: pre_install_check
          changed_when: false

        - name: Display Pre-Install Check Result
          ansible.builtin.debug:
            msg: "{{ target_kb_id }} status before patching: {{ pre_install_check.stdout | trim }}"

        # Only proceed if the patch is not installed
        - name: Apply Patch MSU
          when: "'Not Installed' in pre_install_check.stdout"
          block:
            # 1. Stop Service IIS
            - name: Stop Service IIS before patching
              ansible.windows.win_service:
                name: "W3SVC" # W3SVC คือ World Wide Web Publishing Service ของ IIS
                state: stopped
              ignore_errors: true

            # 2. Apply patch using win_hotfix
            - name: Apply patch using win_hotfix
              ansible.windows.win_hotfix:
                source: "{{ target_msu_path }}"
                state: present
              register: hotfix_result

            # 3. Restart Server
            - name: Reboot Windows System if required by the patch
              ansible.windows.win_reboot:
                reboot_timeout: 18000
              when: hotfix_result.reboot_required | default(false)

        # Post-Installation Check for MSU
        - name: Verify patch after installation (Post-Install Check)
          ansible.windows.win_shell: |
            Try {
                $hotfix = Get-HotFix -Id {{ target_kb_id }} -ErrorAction Stop
                Write-Output "Installed"
            } Catch {
                Write-Output "Not Installed"
                exit 1
            }
          register: post_install_check
          changed_when: false
          failed_when: "'Not Installed' in post_install_check.stdout"

        - name: Display Post-Install Check Result
          ansible.builtin.debug:
            msg: "{{ target_kb_id }} status after patching: {{ post_install_check.stdout | trim }}"


    # ==========================================
    # Patch Method 2: WSUS (Install specific ID or select first ID)
    # ==========================================
    - name: Executing Patch via WSUS
      when: patch_method == 'wsus'
      block:
        # ---------------------------------------------------------
        # [NEW] WSUS Server Auto-Approval & Download Wait
        # ---------------------------------------------------------
        - name: Manage WSUS Approval and Download (Only if target KBs are specified)
          when: target_kb_id | default('', true) != ''
          delegate_to: "{{ wsus_server_ip }}" # IP ของ WSUS Server
          run_once: true         # ทำแค่ครั้งเดียวพอ ไม่ยิงครบทุกเครื่อง
          block:
            - name: Auto-Approve target KBs on WSUS Server
              ansible.windows.win_shell: |
                Import-Module UpdateServices -ErrorAction Stop
                $wsus = Get-WsusServer
                $groupName = "{{ wsus_computer_group | default('All Computers') }}"
                $targetGroup = $wsus.GetComputerTargetGroups() | Where-Object { $_.Name -eq $groupName }
                
                if (-not $targetGroup) {
                    Write-Output "ERROR: Target group '$groupName' not found on WSUS."
                    exit 1
                }

                
                # เอา KB ที่เป็น comma มาตัดคำและลบคำว่า KB ออกเพื่อให้ SearchUpdates หาง่ายขึ้น
                $kbString = "{{ target_kb_id }}"
                $kbList = $kbString.Split(',') | ForEach-Object { $_.Trim().ToUpper().Replace('KB', '') }
                
                foreach ($kb in $kbList) {
                    if (-not [string]::IsNullOrWhiteSpace($kb)) {
                        Write-Output "Searching WSUS for KB$kb..."
                        # หา Update ที่เนื้อหามีเลข KB นี้
                        $updates = $wsus.SearchUpdates($kb)
                        foreach ($update in $updates) {
                            if (-not $update.IsDeclined) {
                                Write-Output "Approving: $($update.Title)"
                                if ($update.RequiresLicenseAgreementAcceptance) {
                                    $update.AcceptLicenseAgreement()
                                }
                                $update.Approve("Install", $targetGroup) | Out-Null
                            }
                        }
                    }
                }
              register: wsus_approval_output

            - name: Display WSUS Approval Result
              ansible.builtin.debug:
                msg: "{{ wsus_approval_output.stdout_lines }}"

            - name: Wait for WSUS to finish downloading the approved updates
              ansible.windows.win_shell: |
                Import-Module UpdateServices -ErrorAction Stop
                $wsus = Get-WsusServer
                $status = $wsus.GetStatus()
                
                $needingFiles = $status.UpdatesNeedingFilesCount
                $totalMB     = [math]::Round($status.TotalBytesToDownload / 1MB, 2)
                $downloadedMB = [math]::Round($status.TotalBytesDownloaded / 1MB, 2)
                
                Write-Output "Updates needing files : $needingFiles"
                Write-Output "Downloaded            : $downloadedMB MB / $totalMB MB"
                
                if ($needingFiles -gt 0) {
                    exit 1
                }
                exit 0
              register: wsus_download_status
              until: wsus_download_status.rc == 0
              retries: 120    # รอสูงสุด 120 x 30 วินาที = 1 ชั่วโมง
              delay: 30       # เช็คสถานะการดาวน์โหลดบน WSUS ทุกๆ 30 วินาที
              ignore_errors: true

        # ---------------------------------------------------------
        # Start Client Installation Process
        # ---------------------------------------------------------
        - name: Search for available updates from WSUS on Target Client
          ansible.windows.win_updates:
            state: searched
          register: wsus_search

        - name: Set target update list fact
          ansible.builtin.set_fact:
            # รองรับการส่งค่าหลายตัวผ่าน comma เช่น KB5074975,KB5074976 โดยดึงจาก target_kb_id หรือ target_wsus_id
            updates_to_install: >-
              {%- if target_kb_id | default('', true) != '' -%}
              {{ target_kb_id.split(',') | map('trim') | list }}
              {%- elif target_wsus_id | default('', true) != '' -%}
              {{ target_wsus_id.split(',') | map('trim') | list }}
              {%- elif wsus_search.updates is defined and wsus_search.updates | length > 0 -%}
              [ "{{ wsus_search.updates[0].id }}" ]
              {%- else -%}
              []
              {%- endif -%}

        # Only proceed to stop services and install if an update ID is determined
        - name: Install WSUS Update
          when: updates_to_install | length > 0
          block:
            # 1. Stop Service IIS
            - name: Stop Service IIS before patching
              ansible.windows.win_service:
                name: "W3SVC"
                state: stopped
              ignore_errors: true
              
            # 2. Install the update
            - name: Install the specified updates from WSUS
              ansible.windows.win_updates:
                state: installed
                accept_list: "{{ updates_to_install }}"
              register: wsus_install

            # 3. Restart Server
            - name: Reboot Windows System if required by WSUS update
              ansible.windows.win_reboot:
                reboot_timeout: 18000
              when: wsus_install.reboot_required | default(false)

        - name: Display WSUS Install Status
          ansible.builtin.debug:
            msg: "WSUS updates {{ 'installed: ' + (updates_to_install | join(', ')) if updates_to_install | length > 0 else 'not found. No updates required.' }}"

    # ==========================================
    # Shared Post Tasks (Starts services and validations)
    # ==========================================
    - name: Shared Post Tasks Implementation
      block:
        # 4. Check Service IIS is running
        - name: Check Service IIS status
          ansible.windows.win_service:
            name: "W3SVC"
          register: iis_service_status

        - name: Display IIS Service Status
          ansible.builtin.debug:
            msg: "Service W3SVC (IIS) is currently {{ iis_service_status.state }}"
